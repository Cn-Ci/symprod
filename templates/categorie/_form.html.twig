<div class="container">
    <center>
        <div class="col-6 text-center">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-success">{{ button_label|default('Save') }}</button>
            {{ form_end(form) }}
        </div>
    <center>
</div>
